{
	"id": "org.webosports.app.atlas",
	"version": "0.9.12",
	"vendor": "WebOS Ports",
	"changeLog": "0.9.12:\n• Photos, videos and music open in their own apps again. Atlas was claiming every local file:// link — one line in its app manifest that outranks mime and extension handlers, so anything opened from a file manager came to the browser instead of Photos, Video or Music. Atlas keeps the web and FTP roles it should have, and hands local media back.\n• Nothing else changed. The app and the browser engine are otherwise identical to 0.9.11.\n\n0.9.11:\n• The engine hang is fixed at the source. The freeze known as the “GPU wedge” — after heavy pages, site menus, or a few minutes of browsing, no page would ever load again until the engine was restarted — was never the GPU. It was three defects in the engine’s own plumbing, each proven and fixed: the frame channel handed WebKit a socket and then closed it again after WebKit already owned it, so the engine ended up listening on someone else’s socket; every rendered frame was acknowledged into a buffer nothing ever emptied, so after a few hundred frames on one page the engine blocked forever mid-frame; and a leftover debug hook sat on the exact signal JavaScriptCore uses to coordinate garbage collection, deadlocking the engine’s JS heap.\n• Before/after on the same device: the old engine froze after 8 page loads (and, separately, after ~5 minutes on an animated page); the fixed engine cleared 64 heavy page loads and a 12-minute animation soak without a hiccup.\n• The Restart Browser Engine menu item stays — belt and braces — but you should not need it anymore.\n\n0.9.10:\n• Installs on webOS 3.1.0. On the community webOS CE 3.1.0 build the install failed outright — the app folder was left empty and no icon appeared in the launcher — because Atlas asked the package manager for the TLS 1.3 Updates bundle, which 3.1.0 does not offer (it already has that stack built in). Atlas now accepts either the bundle or the OpenSSL 1.1 package underneath it, so the same download installs on webOS 3.0.5 and 3.1.0.\n• The engine no longer restarts itself while you are reading. A watchdog meant to rescue a hung engine could not tell a hung one from an idle one, and was shutting the engine down — and reloading your cards — during normal use, sometimes twice in a few minutes. That watchdog is gone.\n• New menu item: Restart Browser Engine. If the browser ever stops responding, this restarts the engine and puts you back on the page you were reading — no reboot, no reinstall. Atlas does not try to guess when that has happened; you decide.\n• The browser engine itself is unchanged — byte-for-byte the 0.9.8 / 0.9.9 engine.\n\n0.9.9:\n• Fresh-install fix — on some devices the installer failed to put the GPU driver in place (libEGL.so.1 / libGLESv2.so.2), leaving a browser that opened but rendered nothing. The installer now falls back to the copy bundled in the package, stages the unversioned libEGL.so the Adreno driver asks for, and reports a clear error instead of failing silently.\n• Installing no longer interrupts itself — the installer used to restart the webOS UI the moment Atlas finished, which in Preware killed the installer mid-run and abandoned any packages queued behind it. Feed builds now leave that restart to the package manager, which does it once at the end; standalone downloads still handle it themselves.\n\n0.9.8:\n• Recovers from engine hangs on its own — GPU-heavy pages (site pop-up menus, rotation) could hang the browser engine: pages simply stopped loading, and the only way out was a reboot. Atlas now spots the hang in about a minute, restarts the engine, and reloads the page you were on. Measured 14m30s → 63s.\n• The card no longer strands after an engine restart — it tells you what is happening and comes back to the page you were reading.\n\n0.9.7:\n• Motion & orientation sensors — DeviceOrientation (now full 3-axis incl. a tilt-compensated compass heading) and DeviceMotion fire with real accelerometer + gyroscope data from the TouchPad's sensors.\n• Generic Sensor API — Accelerometer, Gyroscope, Magnetometer and LinearAccelerationSensor, backed by the same live sensor data (html5test Sensors 5/5).\n\n0.9.6:\n• AVIF and JPEG-XL image decoding — next-gen image formats now display (via dav1d / libjxl).\n• Faster first-page load — engine binary stripped 104→67 MB, so a cold start has far less to fault in; once warm, subsequent pages/reloads fly.\n• Persistent media plugin registry — no GStreamer rescan lag on the first browse after each reboot.\n• MIME handler cleanup — Atlas opens image / audio / video and file/ftp links, but no longer competes for the default-browser or plain-text role.\n• TLS: upgraded the network crypto stack to OpenSSL 3.5 LTS (from 3.0) — HTTPS and WebRTC (DTLS) verified.\n\n0.9.5:\n• New web APIs: Payment Request, Gamepad, Web App Manifest, Web Speech synthesis (4 Flite voices), form date/time/color pickers, Encrypted Media (ClearKey).\n• WOFF2 web fonts — modern sites render their fonts instead of blank text.\n• Images: JPEG + WebP canvas export (toDataURL).\n• Media: AAC, Ogg/Vorbis/Opus, WMA/WMV, Ogg Theora, MPEG-TS; autoplay-with-sound toggle.\n• WebRTC: peer connections, data channels, and receive-path audio + video; camera getUserMedia.\n• Scrolling: tall / JavaScript-grown pages now scroll all the way; fixed fast-flick stripe artifacts and portrait-rotation shear.\n• Fixes: type in the address bar after a page auto-focuses an input; memory-pressure valve (no more heavy-page reboots); WebProcess leak on card close; storage/cache moved off VFAT (IndexedDB); injected-bundle deploy consistency.\n• App: password + autofill managers, start-page grid, text-selection toolbar, cross-app paste, translate option, App Museum II auto-update.\n• Self-contained IPK bundling the full current engine. html5test.co ~449 → ~535.",
	"type": "web",
	"main": "index.html",
	"title": "Atlas Web",
	"keywords" : [ "web", "Browser", "Blazer", "Internet", "Atlas", "Osiris", "shindig", "explorer" ],
	"icon": "icon.png",
	"splashicon": "icon-256x256.png",
	"uiRevision": 2,
	"tapToShareSupported":true,
	"mimeTypes": [
{ "urlPattern": "^ftp:" },
{ "mime": "application/xhtml+xml", "extension": "xhtml" },
{ "mime": "application/xml", "extension": "xml" },
{ "mime": "text/plain", "extension": "txt" },
{ "mime": "text/html", "extension": "html" },
{ "mime": "text/json", "extension": "json" },
{ "mime": "image/svg+xml", "extension": "svg" },
{ "mime": "image/x-icon", "extension": "ico" }
],
	"universalSearch": {
		"dbsearch": {
			"displayName":"Bookmarks & History",
			"url":"org.webosports.app.atlas",
			"launchParam":"url",
			"launchParamDbField":"url",
			"displayFields": ["title", "url"],
			"dbQuery": [{"method":"search","params":{"query":{"from":"com.palm.browserbookmarks:1","where":[{"prop":"searchText","op":"?","val":"","collate":"primary"}],"limit":20}}},{"method":"search","params":{"query":{"from":"com.palm.browserhistory:1","where":[{"prop":"searchText","op":"?","val":"","collate":"primary"}],"limit":50}}}],
			"batchQuery":true
		}
	}
}
